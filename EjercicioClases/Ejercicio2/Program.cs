using System; 

namespace NumeroPositonegativo
{
    class Program
    {
        static void Main (string[] args)
        {
            decimal numero = 0; 
            int positivo= 0; 
            int negativo = 0; 

            Console.Write("Ingresa un número: ");
            numero = Convert.ToDecimal(Console.ReadLine()); 

            if (numero > 0)
            {
                Console.WriteLine("el numero es positivo"); 
                positivo++; 
            } 
            else
            {
                Console.WriteLine("El numero es negativo");
                negativo++;  
            }

            Console.Write("Ingresa un número: ");
            numero = Convert.ToDecimal(Console.ReadLine()); 

            if (numero > 0)
            {
                Console.WriteLine("el numero es positivo"); 
                positivo++; 
            } 
            else
            {
                Console.WriteLine("El numero es negativo");
                negativo++;  
            }

            Console.Write("Ingresa un número: ");
            numero = Convert.ToDecimal(Console.ReadLine()); 

            if (numero > 0)
            {
                Console.WriteLine("el numero es positivo"); 
                positivo++; 
            } 
            else
            {
                Console.WriteLine("El numero es negativo");
                negativo++;  
            }

            Console.Write("Ingresa un número: ");
            numero = Convert.ToDecimal(Console.ReadLine()); 

            if (numero > 0)
            {
                Console.WriteLine("el numero es positivo"); 
                positivo++; 
            } 
            else
            {
                Console.WriteLine("El numero es negativo");
                negativo++;  
            }

            Console.Write("Ingresa un número: ");
            numero = Convert.ToDecimal(Console.ReadLine()); 

            if (numero > 0)
            {
                Console.WriteLine("el numero es positivo"); 
                positivo++; 
            } 
            else
            {
                Console.WriteLine("El numero es negativo");
                negativo++;  
            }

            Console.Write("Ingresa un número: ");
            numero = Convert.ToDecimal(Console.ReadLine()); 

            if (numero > 0)
            {
                Console.WriteLine("el numero es positivo"); 
                positivo++; 
            } 
            else
            {
                Console.WriteLine("El numero es negativo");
                negativo++;  
            }

            Console.Write("Ingresa un número: ");
            numero = Convert.ToDecimal(Console.ReadLine()); 

            if (numero > 0)
            {
                Console.WriteLine("el numero es positivo"); 
                positivo++; 
            } 
            else
            {
                Console.WriteLine("El numero es negativo");
                negativo++;  
            }

            Console.Write("Ingresa un número: ");
            numero = Convert.ToDecimal(Console.ReadLine()); 

            if (numero > 0)
            {
                Console.WriteLine("el numero es positivo"); 
                positivo++; 
            } 
            else
            {
                Console.WriteLine("El numero es negativo");
                negativo++;  
            }

            Console.Write("Ingresa un número: ");
            numero = Convert.ToDecimal(Console.ReadLine()); 

            if (numero > 0)
            {
                Console.WriteLine("el numero es positivo"); 
                positivo++; 
            } 
            else
            {
                Console.WriteLine("El numero es negativo");
                negativo++;  
            }

            Console.Write("Ingresa un número: ");
            numero = Convert.ToDecimal(Console.ReadLine()); 

            if (numero > 0)
            {
                Console.WriteLine("el numero es positivo"); 
                positivo++; 
            } 
            else
            {
                Console.WriteLine("El numero es negativo");
                negativo++;  
            }

            Console.WriteLine($"positivo: {positivo}");
            Console.WriteLine($"Negativo: {negativo}");  


        }
    }
}
